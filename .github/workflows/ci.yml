name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: ${{ github.run_id }}
          POSTGRES_DB: termsinator_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres -d termsinator_test"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgres://postgres:${{ github.run_id }}@127.0.0.1:5432/termsinator_test?sslmode=disable
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache: true
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: processor/requirements.txt
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.2
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Go tests
        run: go test -race ./...
      - name: Processor tests
        run: |
          pip install -r processor/requirements.txt
          PYTHONPATH=processor python -m unittest discover -s processor/tests -v
      - name: Frontend install
        run: pnpm --dir frontend install --frozen-lockfile
      - name: Package and validate extension
        run: |
          scripts/package-extension.sh
          unzip -t frontend/public/downloads/termsinator-extension.zip
      - name: Frontend check and build
        run: pnpm --dir frontend build
      - name: Validate JSON contracts
        run: for file in schemas/*.json extension/manifest.json; do python3 -m json.tool "$file" >/dev/null; done
      - name: Validate Compose
        env:
          POSTGRES_PASSWORD: ${{ github.run_id }}
        run: docker compose -f compose.production.yml config --quiet
